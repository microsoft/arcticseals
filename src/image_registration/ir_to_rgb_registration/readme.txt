To run you need to install these python packages: 

opencv-python
opencv-python-contrib
matplotlib

The code computes a transform from IR to RGB image and illustrates how to use it to:

- warp the IR image so that it lines up with the RGB
- warp a hotspot, so that it lines up with the RGB
- save and RGB-IR image as RGBA